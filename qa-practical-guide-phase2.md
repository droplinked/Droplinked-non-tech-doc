# راهنمای عملی QA - Droplinked

> **مرحله ۲: Health Check، Quality Gates، Migration و API Test Automation**
> **تاریخ:** بهمن ۱۴۰۴

---

## فهرست

1. [Health Check عملی برای Droplinked](#1-health-check-عملی-برای-droplinked)
2. [Quality Gates - چک‌لیست QA قبل از مرج](#2-quality-gates---چکلیست-qa-قبل-از-مرج)
3. [پروسه تست Database Migration](#3-پروسه-تست-database-migration)
4. [اتوماسیون تست API با TDD](#4-اتوماسیون-تست-api-با-tdd)

---

## 1. Health Check عملی برای Droplinked

### 🎯 ساختار محصول Droplinked

Droplinked ۴ بخش اصلی داره:
- **Dashboard** (droplinked.com) - پنل مدیریت فروشگاه
- **Shopfront** (droplinked.io/shopname) - فروشگاه عمومی
- **Checkout** (checkout.droplinked.com) - صفحه پرداخت
- **API** - سرویس‌های backend

### 📋 چک‌لیست Health Check (روزانه/بعد از deploy)

#### الف) Dashboard (پنل مدیریت)

**چک‌های سطح ۱ (حیاتی - هر ۵ دقیقه):**
- [ ] آدرس `https://droplinked.com` باز می‌شه؟
- [ ] صفحه لاگین لود می‌شه؟
- [ ] فرم لاگین کار می‌کنه؟ (با یه اکانت تستی چک کن)
- [ ] داشبورد بعد لاگین لود می‌شه؟

**چک‌های سطح ۲ (مهم - هر ۱ ساعت):**
- [ ] محصول جدید ساخته می‌شه؟
- [ ] عکس محصول آپلود می‌شه؟
- [ ] تغییرات ذخیره می‌شه؟
- [ ] API داشبورد پاسخ 200 میده؟

**ابزار پیشنهادی:**
- **UptimeRobot** (رایگان) - هر ۵ دقیقه چک می‌کنه
- **curl** (رایگان) - برای چک سریع دستی

**مثال curl:**
```bash
# چک کردن سلامت داشبورد
curl -s -o /dev/null -w "%{http_code}" https://droplinked.com/health
# باید 200 برگردونه
```

---

#### ب) Shopfront (فروشگاه مشتری)

**چک‌های سطح ۱:**
- [ ] آدرس `https://droplinked.io/test-shop` باز می‌شه؟
- [ ] لیست محصولات نمایش داده می‌شه؟
- [ ] عکس محصولات لود می‌شه؟
- [ ] دکمه "Add to Cart" کار می‌کنه؟

**چک‌های سطح ۲:**
- [ ] صفحه جزئیات محصول باز می‌شه؟
- [ ] سبد خرید (Cart) به درستی نشون داده می‌شه؟
- [ ] قیمت‌ها درست نمایش داده می‌شن؟

---

#### ج) Checkout (سیستم پرداخت)

**چک‌های سطح ۱ (خیلی خیلی مهم):**
- [ ] آدرس `https://checkout.droplinked.com` باز می‌شه؟
- [ ] صفحه اطلاعات مشتری لود می‌شه؟
- [ ] فرم آدرس کار می‌کنه؟
- [ ] روش‌های پرداخت نمایش داده می‌شن؟

**چک‌های سطح ۲ (مهم برای درآمد):**
- [ ] محاسبه قیمت (Cart Calculation) درسته؟
- [ ] کد تخفیف (Coupon) اعمال می‌شه؟
- [ ] محاسبه مالیات (Tax) درسته؟
- [ ] هزینه ارسال (Shipping) محاسبه می‌شه؟
- [ ] پرداخت تستی (Sandbox) کار می‌کنه؟
- [ ] سفارش توی دیتابیس ثبت می‌شه؟

**چک‌های سطح ۳ (موتورهای پرداخت):**
- [ ] Commission Engine کار می‌کنه؟ (تقسیم درآمد)
- [ ] Merchant Share درسته؟
- [ ] Order Creation Engine خطا نمیده؟
- [ ] Payment Routing درسته؟

---

#### د) API (بک‌اند)

**چک‌های سطح ۱:**
- [ ] `/health` endpoint پاسخ 200 میده؟
- [ ] `/api/products` لیست محصول رو برمی‌گردونه؟
- [ ] `/api/orders` سفارشات رو نشون میده؟
- [ ] دیتابیس وصله؟ (یه کوئری ساده تست کن)

**چک‌های سطح ۲:**
- [ ] Redis وصله؟ (اگه استفاده می‌کنی)
- [ ] زمان پاسخ API زیر 500ms هست؟
- [ ] Error Rate زیر ۱٪ هست؟

---

### 🤖 چطور اتوماتیک کنیم؟

**راه‌حل ۱: GitHub Actions (رایگان)**

یه فایل بساز به اسم `.github/workflows/health-check.yml`:

```yaml
name: Health Check

on:
  schedule:
    - cron: '*/5 * * * *'  # هر ۵ دقیقه
  workflow_dispatch:  # امکان اجرای دستی

jobs:
  check-dashboard:
    runs-on: ubuntu-latest
    steps:
      - name: Check Dashboard
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://droplinked.com/health)
          if [ $STATUS -ne 200 ]; then
            echo "Dashboard is down! Status: $STATUS"
            exit 1
          fi
      
      - name: Notify Slack if failed
        if: failure()
        run: |
          curl -X POST $SLACK_WEBHOOK \
            -H 'Content-type: application/json' \
            -d '{"text":"🚨 Dashboard is DOWN!"}'

  check-checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Check Checkout
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://checkout.droplinked.com/health)
          if [ $STATUS -ne 200 ]; then
            echo "Checkout is down! Status: $STATUS"
            exit 1
          fi
```

**راه‌حل ۲: UptimeRobot (رایگان)**

۱. برو uptimerobot.com و ثبت‌نام کن
۲. "New Monitor" بزن
۳. آدرس بده: `https://droplinked.com/health`
۴. interval: ۵ دقیقه
۵. اگه fail شد، ایمیل/اسلک بفرست

**راه‌حل ۳: اسکریپت ساده Node.js (رایگان)**

یه فایل بساز `health-check.js`:

```javascript
const axios = require('axios');

const services = [
  { name: 'Dashboard', url: 'https://droplinked.com/health' },
  { name: 'Checkout', url: 'https://checkout.droplinked.com/health' },
  { name: 'API', url: 'https://api.droplinked.com/health' }
];

async function checkHealth() {
  for (const service of services) {
    try {
      const response = await axios.get(service.url, { timeout: 5000 });
      console.log(`✅ ${service.name}: OK (${response.status})`);
    } catch (error) {
      console.error(`❌ ${service.name}: FAILED`);
      // ارسال به Slack
      await notifySlack(service.name);
    }
  }
}

// هر ۵ دقیقه اجرا بشه
setInterval(checkHealth, 5 * 60 * 1000);
checkHealth(); // اجرای اولیه
```

و با `node health-check.js` اجراش کن (روی یه سرور یا Railway).

---

## 2. Quality Gates - چک‌لیست QA قبل از مرج

### 🎯 چرا Quality Gates مهمه؟

قبل از اینکه فیچر جدید به `main` بره و برسه به دست کاربر، باید چند تا در (Gate) رو رد کنه. اگه یه دری رو رد نکنه، مرج بلاک می‌شه.

### 📋 فلوچارت Quality Gates

```
┌──────────────────────────────────────────────┐
│  برنامه‌نویس کد رو می‌نویسه                   │
│  (روی برنچ feature-x)                         │
└──────────────┬───────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────┐
│  Gate 1: Code Review                          │
│  □ کد تمیزه؟ (Lint pass)                      │
│  □ Type check OK؟                             │
│  □ ۲ نفر review کردن؟                         │
│  □ تست unit نوشته شده؟                        │
└──────────────┬───────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────┐
│  Gate 2: Tests Pass                           │
│  □ Unit tests پاس شدن (۸۰٪+)؟                │
│  □ Integration tests پاس شدن؟                 │
│  □ Build موفق بوده؟                           │
└──────────────┬───────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────┐
│  Gate 3: Deploy to Staging                    │
│  □ روی Staging deploy شد؟                     │
│  □ Smoke test پاس شد؟                         │
└──────────────┬───────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────┐
│  Gate 4: QA Testing                           │
│  □ QA تست کرده؟                               │
│  □ تست کیس‌ها پاس شدن؟                        │
│  □ باگ‌های بحرانی رفع شدن؟                    │
└──────────────┬───────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────┐
│  ✅ APPROVED FOR MERGE                        │
└──────────────────────────────────────────────┘
```

### ✅ چک‌لیست دقیق QA قبل از مرج

#### Gate 1: Code Review Checklist

**برنامه‌نویس قبل از PR باید چک کنه:**
- [ ] `npm run lint` پاس می‌شه (خطای فرمت نداره)
- [ ] `npm run typecheck` پاس می‌شه (خطای type نداره)
- [ ] Unit tests نوشتم (`npm run test`)
- [ ] Coverage کم نشده (بالای ۸۰٪)
- [ ] کامنت گذاشتم برای قسمت‌های پیچیده
- [ ] TODO resolve کردم

**Reviewer باید چک کنه:**
- [ ] کد خواناست
- [ ] منطق درسته
- [ ] Security issue نداره
- [ ] تست کافی نوشته شده

#### Gate 2: Automated Tests

**باید توی CI/CD باشه (GitHub Actions):**

```yaml
name: Quality Gates

on:
  pull_request:
    branches: [main, staging]

jobs:
  lint-and-type:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Type Check
        run: npm run typecheck

  unit-tests:
    runs-on: ubuntu-latest
    needs: lint-and-type
    steps:
      - uses: actions/checkout@v4
      - name: Run tests with coverage
        run: npm run test:coverage
      - name: Check coverage threshold
        run: |
          COVERAGE=$(cat coverage/coverage-summary.json | jq '.total.lines.pct')
          if (( $(echo "$COVERAGE < 80" | bc -l) )); then
            echo "Coverage $COVERAGE% is below 80%"
            exit 1
          fi
```

#### Gate 3: Staging Deploy

**بعد از مرج به `staging`:**
- [ ] کد روی Staging deploy شد
- [ ] Health check پاس شد
- [ ] Smoke test سریع انجام شد:
  - [ ] سایت بالاست
  - [ ] لاگین کار می‌کنه
  - [ ] فیچر جدید کار می‌کنه

#### Gate 4: QA Manual Testing

**QA باید توی Stgage اینا رو چک کنه:**

**الف) تست‌های کلی:**
- [ ] فیچر جدید کار می‌کنه (Happy path)
- [ ] ورودی‌های نامعتبر خطا میده (Validation)
- [ ] Edge cases تست شده
- [ ] روی Chrome OKه
- [ ] روی Firefox OKه
- [ ] روی Safari OKه
- [ ] روی موبایل OKه

**ب) تست‌های خاص Droplinked:**

اگه فیچر مربوط به Checkout هست:
- [ ] Cart Calculation درسته؟
- [ ] Coupon Code اعمال می‌شه؟
- [ ] Tax درست محاسبه می‌شه؟
- [ ] Shipping اضافه می‌شه؟
- [ ] Payment sandbox کار می‌کنه؟
- [ ] Order توی دیتابیس ثبت می‌شه؟
- [ ] Commission درست تقسیم می‌شه؟
- [ ] Confirmation email میره؟

اگه فیچر مربوط به Dashboard هست:
- [ ] Product create می‌شه؟
- [ ] Product update می‌شه؟
- [ ] Product delete می‌شه؟
- [ ] Image upload می‌شه؟
- [ ] Changes save می‌شه؟
- [ ] API درست کار می‌کنه؟

اگه فیچر مربوط به Shopfront هست:
- [ ] Products نمایش داده می‌شن؟
- [ ] Search کار می‌کنه؟
- [ ] Filter کار می‌کنه؟
- [ ] Cart add/remove کار می‌کنه؟
- [ ] Checkout flow کامل می‌شه؟

**ج) Regression Testing:**
- [ ] فیچرهای قدیمی هنوز کار می‌کنن؟
- [ ] چیزی نشکسته؟

**د) Documentation:**
- [ ] تست کیس‌ها آپدیت شدن
- [ ] باگ‌ها ثبت شدن (توی Linear/GitHub Issues)
- [ ] تغییرات مستند شدن

**اگه همه اینا پاس شدن:**
- [ ] QA تایید می‌کنه (comment می‌ذاره "QA Passed ✅")
- [ ] Product Manager تایید می‌کنه
- [ ] حالا می‌تونه به `main` مرج بشه

---

## 3. پروسه تست Database Migration

### 🎯 چرا Migration خطرناکه؟

وقتی ساختار دیتابیس عوض می‌شه (مثلاً ستون جدید اضافه می‌شه)، ممکنه:
- داده‌های قدیمی خراب بشن
- سایت Down بشه
- Rollback سخت یا نشدنی باشه

### 📋 پروسه ۴ مرحله‌ای

#### مرحله ۱: قبل از نوشتن Migration

**دولوپر باید:**
- [ ] مشخص کنه چه تغییری می‌خواد بده (مثلاً "اضافه کردن ستون email_verified")
- [ ] بگه این تغییر backward compatible هست یا نه؟
  - **Backward compatible** = کد قدیمی با دیتابیس جدید کار می‌کنه
  - **Breaking change** = کد قدیمی خراب می‌شه
- [ ] برآورد کنه چقدر طول می‌کشه (اگه زیاد باشه، downtime داریم)

**انواع Migration:**

| نوع | مثال | ریسک | روش تست |
|-----|------|------|---------|
| **ساده** | اضافه کردن ستون nullable | کم | تست روی Staging |
| **متوسط** | اضافه کردن ستون not null | متوسط | Need backfill + تست |
| **پیچیده** | حذف ستون / تغییر نوع داده | بالا | Expand-Contract Pattern |
| **خطرناک** | تغییر Primary Key / جدول بزرگ | خیلی بالا | downtime planned |

#### مرحله ۲: تست محلی (Local)

**دولوپر روی کامپیوتر خودش:**
- [ ] Migration رو اجرا می‌کنه
- [ ] چک می‌کنه error نده
- [ ] داده‌های تستی رو می‌بینه هنوز هستن
- [ ] Rollback رو تست می‌کنه (`migrate:down` یا `migrate:rollback`)
- [ ] زمان اجرا رو اندازه می‌گیره

#### مرحله ۳: تست روی Staging

**بعد از deploy روی Staging:**
- [ ] Migration خودکار اجرا می‌شه
- [ ] QA چک می‌کنه فیچر جدید کار می‌کنه
- [ ] Data Integrity Check انجام می‌شه (داده‌های قدیمی سالم هستن؟)
- [ ] Rollback تست می‌شه

**چک‌لیست Data Integrity:**
```sql
-- مثال چک‌ها
SELECT COUNT(*) FROM orders;  -- تعداد سفارشات تغییر نکرده؟
SELECT COUNT(*) FROM orders WHERE user_id IS NULL;  -- همه سفارشات کاربر دارن؟
SELECT * FROM users WHERE email IS NULL;  -- ایمیل خالی نداریم؟
```

#### مرحله ۴: Deploy به Production

**قبل از deploy:**
- [ ] از دیتابیس Production بک‌آپ گرفته شد
- [ ] پنجره Maintenance انتخاب شده (وقت کم‌ترافیک)
- [ ] تیم آماده Rollback هست

**حین deploy:**
- [ ] Migration اجرا می‌شه
- [ ] Health check انجام می‌شه
- [ ] اگه خطا داد → Rollback فوری

**بعد از deploy:**
- [ ] Health check دوباره
- [ ] Data Integrity Check
- [ ] monitoring برای ۱ ساعت

### 🤖 چطور خودکار کنیم؟

**GitHub Actions برای Migration:**

```yaml
name: Migration Test

on:
  push:
    paths:
      - 'migrations/**'

jobs:
  test-migration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup test database
        run: |
          # restore production-like data
          psql -h localhost -U postgres < test/fixtures/production-subset.sql
      
      - name: Run migration
        run: npm run migrate:latest
      
      - name: Test rollback
        run: npm run migrate:rollback
      
      - name: Run migration again
        run: npm run migrate:latest
      
      - name: Validate data integrity
        run: node scripts/validate-data.js
```

### 📝 تمپلیت PR برای Migration

دولوپر باید توی Pull Request بنویسه:

```markdown
## Database Migration

**نوع:** اضافه کردن ستون  
**جدول:** users  
**تغییر:** اضافه کردن ستون `email_verified_at` (timestamp, nullable)  

**Backward Compatible:** ✅ بله  
**زمان اجرا تست:** ۲ ثانیه برای ۱۰۰۰ رکورد  
**دowntime:** ❌ نداریم  

**تست‌ها:**
- [ ] تست محلی پاس شد
- [ ] Rollback تست شد
- [ ] Data Integrity چک شد
```

---

## 4. اتوماسیون تست API با TDD

### 🎯 TDD یعنی چی؟

**Test-Driven Development:**
۱. اول تست API رو بنویس (قبل از نوشتن API!)
۲. ببین تست fail می‌شه (چون API هنوز نیست)
۳. API رو بنویس تا تست pass بشه
۴. refactor کن

### 🏗️ ساختار پیشنهادی

**فولدربندی:**
```
project/
├── src/
│   └── api/
│       └── products.js
├── tests/
│   └── api/
│       └── products.test.js  ← تست‌ها اینجا
├── test-data/
│   └── fixtures/
│       └── products.json     ← داده‌های تست
└── package.json
```

### 📋 راه‌حل عملی: SuperTest + Jest (رایگان)

**مرحله ۱: نصب ابزارها**

```bash
npm install --save-dev jest supertest
```

**مرحله ۲: نوشتن تست (قبل از API!)**

فایل: `tests/api/products.test.js`

```javascript
const request = require('supertest');
const app = require('../../src/app');  // express app

describe('Products API', () => {
  
  test('GET /api/products - باید لیست محصولات رو برگردونه', async () => {
    const response = await request(app)
      .get('/api/products')
      .expect(200);
    
    expect(response.body).toHaveProperty('products');
    expect(Array.isArray(response.body.products)).toBe(true);
  });

  test('GET /api/products/:id - باید یه محصول خاص رو برگردونه', async () => {
    const response = await request(app)
      .get('/api/products/123')
      .expect(200);
    
    expect(response.body).toHaveProperty('id', 123);
    expect(response.body).toHaveProperty('name');
    expect(response.body).toHaveProperty('price');
  });

  test('POST /api/products - باید محصول جدید بسازه', async () => {
    const newProduct = {
      name: 'Test Product',
      price: 99.99
    };
    
    const response = await request(app)
      .post('/api/products')
      .send(newProduct)
      .expect(201);
    
    expect(response.body).toHaveProperty('id');
    expect(response.body.name).toBe('Test Product');
  });

  test('POST /api/products - باید خطا بده اگه name نباشه', async () => {
    const invalidProduct = {
      price: 99.99
    };
    
    await request(app)
      .post('/api/products')
      .send(invalidProduct)
      .expect(400);
  });
});
```

**مرحله ۳: اجرای تست (باید fail بشه چون API نیست!)**

```bash
npm test
# Expected: FAIL (API not implemented yet)
```

**مرحله ۴: نوشتن API**

فایل: `src/api/products.js`

```javascript
const express = require('express');
const router = express.Router();

// GET /api/products
router.get('/', (req, res) => {
  res.json({ products: [] });
});

// GET /api/products/:id
router.get('/:id', (req, res) => {
  res.json({ 
    id: parseInt(req.params.id),
    name: 'Sample Product',
    price: 99.99
  });
});

// POST /api/products
router.post('/', (req, res) => {
  const { name, price } = req.body;
  
  if (!name) {
    return res.status(400).json({ error: 'Name is required' });
  }
  
  res.status(201).json({
    id: Date.now(),
    name,
    price
  });
});

module.exports = router;
```

**مرحله ۵: اجرای دوباره تست (باید pass بشه!)**

```bash
npm test
# Expected: PASS ✅
```

### 🤖 اتوماتیک کردن توی CI/CD

**GitHub Actions:**

```yaml
name: API Tests

on:
  pull_request:
    branches: [main, staging]
  push:
    branches: [main]

jobs:
  api-tests:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: test
        ports:
          - 5432:5432
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Setup test database
        run: |
          npm run db:migrate
          npm run db:seed
      
      - name: Run API tests
        run: npm run test:api
      
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: test-results.xml
```

### 📊 کانتریبیوشن تیم

**برای هر API جدید، تیم باید:**

۱. **Product Manager:**
   - acceptance criteria بنویسه (API چه کارهایی باید بکنه)
   - مثال request/response بده

۲. **QA:**
   - تست کیس‌ها رو بنویسه (مثلاً توی فایل `.test.js`)
   - Edge cases رو مشخص کنه

۳. **Developer:**
   - اول تست رو ببینه (fail می‌شه)
   - API رو بنویسه تا تست pass بشه
   - Refactor کنه

۴. **Code Review:**
   - تست‌ها review بشن
   - API review بشه

**قبل از هر اسپرینت:**
- [ ] APIهای جدید مشخص شدن
- [ ] تست کیس‌ها نوشته شدن
- [ ] داده‌های تست آماده شدن

**حین اسپرینت:**
- [ ] تست‌ها اول نوشته می‌شن (TDD)
- [ ] API پیاده‌سازی می‌شه
- [ ] تست‌ها پاس می‌شن

**بعد اسپرینت:**
- [ ] همه API tests پاس شدن
- [ ] Coverage بالای ۸۰٪
- [ ] Documentation آپدیت شد

### 💰 هزینه

| ابزار | هزینه | کاربرد |
|-------|-------|--------|
| Jest | رایگان | فریم‌ورک تست |
| SuperTest | رایگان | تست HTTP API |
| GitHub Actions | رایگان (۲۰۰۰ دقیقه/ماه) | CI/CD |
| **مجموع** | **رایگان** | - |

### ✅ چک‌لیست اجرایی

**هفته اول:**
- [ ] Jest و SuperTest نصب کن
- [ ] یه فولدر `tests/api` بساز
- [ ] ۳ تا API test ساده بنویس
- [ ] تست روی Staging اجرا کن

**هفته دوم:**
- [ ] GitHub Actions کانفیگ کن
- [ ] تست‌ها توی CI/CD اجرا بشن
- [ ] اگه تست Fail شد، مرج بلاک بشه

**هفته سوم:**
- [ ] تیم رو آموزش TDD بده
- [ ] Template PR برای API بساز
- [ ] Code Review چک‌لیست اضافه کن (چک کردن تست‌ها)

**هفته چهارم:**
- [ ] Coverage Report تنظیم کن (هدف: ۸۰٪)
- [ ] Test Data Management رو مرتب کن
- [ ] Documentation بنویس

---

## خلاصه فلوچارت کلی

```
┌─────────────────────────────────────────────────────────────┐
│                    شروع توسعه فیچر جدید                      │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ ۱. Product Manager: Acceptance Criteria مینویسه            │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ ۲. QA: تست کیس‌ها رو مشخص می‌کنه                           │
│    □ تست API نوشته می‌شه (TDD)                               │
│    □ تست manual مشخص می‌شه                                   │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ ۳. Developer: کد رو می‌نویسه                               │
│    □ Unit test پاس می‌شه                                     │
│    □ API test پاس می‌شه                                      │
│    □ Lint و Type check OK                                    │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ ۴. Pull Request به staging                                   │
│    □ Code Review توسط ۲ نفر                                  │
│    □ CI/CD تست‌ها رو اجرا می‌کنه                             │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ ۵. Deploy روی Staging                                        │
│    □ Health Check اجرا می‌شه                                 │
│    □ Migration تست می‌شه (اگه دیتابیس تغییر کرده)           │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ ۶. QA Testing روی Staging                                    │
│    □ تست کیس‌ها اجرا می‌شن                                   │
│    □ Regression تست می‌شه                                    │
│    □ Manual testing کامل                                     │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ ۷. اگه همه چی OK بود:                                        │
│    □ QA: "QA Passed ✅"                                      │
│    □ PM: تایید نهایی                                         │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ ۸. Pull Request به main                                      │
│    □ Deploy به Production                                    │
│    □ Post-deploy Health Check                                │
│    □ Monitoring برای ۱ ساعت                                  │
└─────────────────────────────────────────────────────────────┘
```

---

**تهیه شده برای:** تیم Droplinked  
**تاریخ:** بهمن ۱۴۰۴  
**وضعیت:** آماده اجرا
